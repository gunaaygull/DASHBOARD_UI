/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['images.pexels.com'], // Resmin geldiği domaini buraya ekliyoruz.
    },
  };
  
  module.exports = nextConfig;
  